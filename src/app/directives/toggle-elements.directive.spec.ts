import { ToggleElementsDirective } from './toggle-elements.directive';

describe('ToggleElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleElementsDirective();
    expect(directive).toBeTruthy();
  });
});
